# my first python project

#終端機
#pwd 顯示檔案所在位子
#git init 初始化
#git branch -M main 把目前Git 的主要分支名稱設定成 main
#git status 顯示目前的git狀態

# 每次修改檔案 通常依序使用：
# 1. git status 查看那些檔案被修改、那些檔案準備提交
# 2. git add . 將目前資料夾內所有變更加入下次commit
# 3. git commit -m "簡短描述這次修改" 建立一筆版本紀錄；訊息說明這次完成什麼
# 4. git push 上傳本機commits 到 github / git pull 下載github的最新變更並合併到本機；開始工作前可先執行

#分支操作
# git branch 列出本機分支；目前所在分支前會有 *
# git checkout -b <分支名稱> 建立新分支並立刻切換過去
# git checkout <分支名稱> 切換到指定分之
# git merge <分支名稱> 將指定分支的commits 合併到目前分支；執行 git merge 前，先用 git branch 確認自己已位於要接收變更的分支，例如 main。

